[package]
name = "uml_metamodel"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", features = ["derive", "rc"] }
serde_json = "1.0.145"
chrono = { version = "0.4", features = ["serde"] }
[lib]
path = './lib.rs'